spring:
  application:
    name: wire-me-up-code-test
  datasource:
    url: jdbc:h2:mem:codetest;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  sql:
    init:
      mode: never

server:
  port: 8080

email-config:
  url: https://smtp.example.com/send
  username: mailer-bot
  password: s3cr3t

logging:
  level:
    com.codetest: DEBUG
